fix: tsc errors in convergence-recovery test (lamport arg, payload shape, Operation type)

- mergeLWW now called with 4 args (local, remote, lamport, remoteLamport)
- push_ack payload carries serverLamport from (message as {lamport?: number}).lamport
- buildBatch ops use valid Operation kind (create_node) with proper payload
- tsc --noEmit now clean (0 errors) — was contradicting §13.0 claim
